import { Header, Footer, MainAuth } from "./";

const Authentication = () => {
  return (
    <div className="home-container">
      <Header />
      <MainAuth />
      <Footer />
    </div>
  );
};

export { Authentication };
